# QuestKids .gitignore — generated by WO-001
# Root screenshots & raw artifacts (assets/ 入面嘅截圖會保留)
/*.png
/*.zip
/*-notion-payload.json
/iphone-*.png

# OS / editor
.DS_Store

# Secrets & credentials (defense-in-depth)
*.env
*.env.local
config/local.json
secrets/

# Node / build artifacts
node_modules/
dist/
build/
*.log